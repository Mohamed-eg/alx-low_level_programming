#include <stdio.h>

/**
 * main - Entry point
 *
 * Discription: A C program that prints a text using printf function
 *
 * Return: Always return 0 (Success)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
